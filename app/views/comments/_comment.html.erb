<div id="<%= dom_id comment %>" class="comment">
  <p><%= name(comment.commenter) %></p>
  <p><%= comment.created_at %></p>

  <p><%= comment.body %></p>

  <%= turbo_frame_tag id="#{dom_id(comment)}_likes" do %>
    <%= render "likes/form", likeable: comment %>
  <% end %>
</div>