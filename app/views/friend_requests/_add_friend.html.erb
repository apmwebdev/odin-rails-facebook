<%= form_with model: FriendRequest, method: "post",
  class: "add-friend-form" do |f| %>
  <%= f.hidden_field :recipient_id, value: recipient.id %>
  <%= f.hidden_field :sender_id, value: current_user.id %>
  <%= f.submit "Add friend", name: nil  %>
<% end %>