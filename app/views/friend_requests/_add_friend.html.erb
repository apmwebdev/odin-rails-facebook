<%= form_with model: FriendRequest, method: "post", class: "inline" do |f| %>
  <%= f.hidden_field :recipient_id, value: other_user.id %>
  <%= f.hidden_field :sender_id, value: current_user.id %>
  <%= f.submit "Add friend", name: nil  %>
<% end %>